package Client.src.model;

/**
 * ToDo
 * Could contain login requests, account creation requests, requests for a users library etc
 * A builder method would simplify the process for future expansion
 */
public class DBRequest extends Request {
}
